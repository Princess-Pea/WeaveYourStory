# 像素风素材说明

此目录用于存放像素风游戏素材，包括角色、物品、背景等。

## 替换素材的方法

要替换预览页面中的像素图标，您可以：

1. 将自定义的像素图片放入此目录
2. 图片建议尺寸：32x32 像素或 64x64 像素
3. 使用PNG格式以保持透明背景
4. 更新前端代码中的图标引用路径

## 预留AI生成素材接口

未来可扩展 `/api/v1/asset/pixel` 接口以支持：

- AI生成自定义像素角色
- AI生成场景背景
- AI生成互动元素
- 根据游戏主题生成匹配的像素素材